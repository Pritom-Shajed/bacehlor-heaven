import 'package:flutter/material.dart';

class LandlordsScreen extends StatelessWidget {
  const LandlordsScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Profile'),
    );
  }
}
